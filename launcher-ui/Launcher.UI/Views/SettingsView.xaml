<UserControl x:Class="Launcher.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Game Path" VerticalAlignment="Center" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Settings.GamePath, Mode=TwoWay}" Margin="4" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Mods Path" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Settings.ModsPath, Mode=TwoWay}" Margin="4" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Language" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Settings.Language, Mode=TwoWay}" Margin="4" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Auto Update" VerticalAlignment="Center" />
        <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding Settings.AutoUpdateEnabled, Mode=TwoWay}" Content="Enable" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Backup Path" VerticalAlignment="Center" />
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Settings.BackupPath, Mode=TwoWay}" Margin="4" />

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="10" Margin="0,10,0,0">
            <Button Content="Save" Width="100" Command="{Binding Settings.SaveCommand}" />
            <Button Content="Backup" Width="100" Command="{Binding Settings.BackupCommand}" />
            <Button Content="Restore" Width="100" Command="{Binding Settings.RestoreCommand}" CommandParameter="{Binding Settings.BackupPath}" />
        </StackPanel>
    </Grid>
</UserControl>
